<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 350 80" width="100%" height="100%">
  <!-- FIX (new file): dark-mode counterpart of logo.svg. Same green
       (#10b981) as the light version; the navy (#0f172a) is swapped for
       near-white so the mark reads on a dark background — see Logo.tsx,
       which now shows this instead of CSS dark:invert-ing the original. -->
  <path d="M20 60 L45 15 L60 15 L35 60 Z" fill="#f8fafc" />
  <path d="M45 60 L70 15 L85 15 L60 60 Z" fill="#10b981" />

  <text x="100" y="50" font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif" font-size="34" font-weight="800" fill="#f8fafc" letter-spacing="-1.5">
    Automatyzuj<tspan fill="#10b981">.it</tspan>
  </text>
</svg>
